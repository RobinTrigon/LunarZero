Write a title for this conversation so the user can find it later. Reply with the title only: one line, at most 50 characters, same language as the user, no quotes and no trailing punctuation.

Name the task or topic, not the tools ("Fix login redirect loop", "Rust workspace setup", "Greeting"). Keep file names, identifiers and numbers exact. Drop articles. If the message is just small talk, describe it briefly ("Greeting", "Quick check-in"). Never answer the message and never explain — always output a title.

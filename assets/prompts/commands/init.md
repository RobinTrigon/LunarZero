Create or update `AGENTS.md` at the repository root as a guide for coding agents working here. Look at the build system, package manifests, existing docs and directory layout first, then write concisely:

- what the project is and how it is structured
- the exact commands to build, test, lint and run
- conventions worth knowing (formatting, naming, error handling, commit style)
- anything that repeatedly trips people up

If `AGENTS.md` exists, keep what is still correct and improve the rest. Aim for 30–80 lines; no boilerplate.

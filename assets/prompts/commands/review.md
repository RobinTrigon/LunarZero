Review the current changes in this repository ($ARGUMENTS if given, otherwise the uncommitted diff, or the current branch against the default branch when the tree is clean).

Read the diff with `bash` (`git diff`, `git diff --cached`, `git log`) and open the surrounding code where needed. Report only findings that matter, most severe first: bugs, wrong behaviour, missing error handling, security issues, and clear maintainability problems — each with `path:line`, why it is a problem, and a concrete fix. Mention what looks good in one line at the end. Do not edit files.
